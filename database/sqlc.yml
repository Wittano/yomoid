version: "2"
sql:
  - engine: "postgresql"
    queries: "./queries/*.sql"
    schema: "./migrations/*.sql"
    gen:
      go:
        package: "database"
        out: "../gen/database"
        sql_package: "pgx/v5"